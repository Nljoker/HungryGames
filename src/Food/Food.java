/*package Food;

import Interfaces.FoodInterface;
import UsedButNotNeededAnymore.competingParticipant;

public abstract class Food extends competingParticipant implements FoodInterface{

    @Override
     public void healLifePoints() {
         int healedPoints = 0;
         System.out.println(name() + " has healed their lifepoints by " + healedPoints + "by eating " + foodName());
     }

 }
*/

